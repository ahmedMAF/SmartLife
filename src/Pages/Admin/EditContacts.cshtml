@page
@model SmartLife.Pages.Admin.EditContactsModel

@{
    ViewData["Title"] = "Edit Contact";
}

<!-- Page Title -->
<div class="page-title dark-background">
    <div class="container">
        <h1>@Model.Localizer["adminhome"]</h1>
        <nav class="breadcrumbs">
            <ol>
                <li><a asp-page="/Index">@Model.Localizer["adminhome"]</a></li>
                <li class="current">@Model.Localizer["editadmin"]</li>
            </ol>
        </nav>
    </div>
</div><!-- End Page Title -->

<!--using SmartLife.Utilities;
#line default
tion id="portfolio" class="portfolio section">
    <!-- Section Title -->
    <div class="container section-title" data-aos="fade-up">
        <h2 style="margin-top: 35px;">@Model.Localizer["editcontact"]</h2>
    </div><!-- End Section Title -->
    <div class="container contact section-title" data-aos="fade-up">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-7">
                <form method="post" class="form" data-aos="fade-up" data-aos-delay="200" asp-antiforgery="false">
                    <div class="row gy-4">
                        <div>
                            <label class="w-100 mb-2 ps-2 text-start textar">@Model.Localizer["select"]</label>
                            <select style="border-radius: 5px;" class="form-select select" name="country" required="">
                                <option value="" disabled selected>@Model.Localizer["select"]</option>
                                @foreach (var contact in Model.Contacts)
                                {
                                    <option value="@contact.Country">@SmartLife.Utilities.LocationHelper.GetCountryNameBy2LetterISO(contact.Country)</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-12 text-center">
                            <div class="loading">Loading</div>
                            <div class="error-message"></div>
                            <button type="submit">@Model.Localizer["edit"]</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
<!-- /Admin Section -->
